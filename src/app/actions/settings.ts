// Removed: system-wide settings were a Super Admin concept. Each teacher now
// manages their own profile via updateProfile() in src/app/actions/profile.ts.
// Safe to delete this file (`rm src/app/actions/settings.ts`).
export {};
