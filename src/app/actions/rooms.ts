// Removed: Room is no longer a separate managed entity. Each Group now just
// stores a plain `roomName` string field — see src/app/actions/groups.ts.
// Safe to delete this file (`rm src/app/actions/rooms.ts`).
export {};
