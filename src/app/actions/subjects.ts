// Removed: Subject is no longer a separate managed entity. Each Course now
// just stores a plain `subject` string field — see src/app/actions/courses.ts.
// Safe to delete this file (`rm src/app/actions/subjects.ts`).
export {};
